# DJ Copilot Decision Log

Last updated: 2026-08-11

This file is the concise index of settled project decisions. Detailed architectural choices live in `docs/adr/`. A settled decision is reopened only when new measured or primary-source evidence is recorded.

| ID | Date | Status | Decision | Evidence / detail |
| --- | --- | --- | --- | --- |
| D-001 | 2026-08-09 | accepted | Treat `dj-copilot-codex-master-prompt.md` as the authoritative product specification despite the filename differing from the requested uppercase name | It is the only repository file matching the described DJ Copilot build prompt and was read completely before changes |
| D-002 | 2026-08-09 | accepted | Freeze production dependency and runtime choices until Phase 0 direct-source research and feasibility measurements are complete | Master prompt §4 and Phase 0 gate |
| D-003 | 2026-08-09 | accepted | Use only the official Codex SDK with existing Codex/ChatGPT authentication for production AI; never add an OpenAI API-key provider | Master prompt product boundary |
| D-004 | 2026-08-09 | accepted | Baseline Rekordbox integration is user-selected XML import/export; Rekordbox-owned databases are never written | Master prompt §§1, 8, and 16 |
| D-005 | 2026-08-09 | accepted | Raw audio analysis is local; Codex receives only bounded structured evidence and validated IDs | Master prompt §§1, 5.3, 13, and 16 |
| D-006 | 2026-08-09 | accepted, local compatibility | Use `agents.max_threads = 4` in project config while the installed Codex CLI is 0.144.1; it rejects the current-manual `agents.max_concurrent_threads_per_session` field | Reproduced by `codex features list`; accepted legacy alias verified by the installed CLI and direct current source. Reassess when the CLI is upgraded; do not infer production SDK behavior from this tooling choice |
| D-007 | 2026-08-09 | accepted for Phase 0 | Use official Rekordbox XML as the only integration boundary; keep `pyrekordbox` reference-only until a later version-gated immutable-snapshot adapter proves read-only behavior | Official format docs; `pyrekordbox` `f695541` mutation-capable DB API and tested Rekordbox ceiling 7.0.9 versus installed 7.2.14 |
| D-008 | 2026-08-09 | accepted for Phase 0 spike | Implement Rekordbox XML and generated PCM-WAV feasibility spikes with the Python standard library; do not treat spike code as the production parser/MIR provider | Bounded fixture requirements in P0-009/P0-010 and research source ledger |
| D-009 | 2026-08-09 | accepted distribution policy | Exclude Essentia code/models from the default distributable build under current evidence | Runtime is AGPL-3.0 unless commercially licensed; official model pages conflict between two noncommercial Creative Commons variants |
| D-010 | 2026-08-09 | accepted limitation | Approve no semantic/audio embedding model for the default package yet; metadata/basic feature similarity is the required fallback | All inspected candidates retain license, provenance, safe-loading, arm64, runtime, or packaging blockers |
| D-011 | 2026-08-09 | accepted for Phase 0 spike | Exercise exactly `@openai/codex-sdk@0.146.0` with its matching packaged runtime and an isolated project lockfile; do not upgrade or override it with the ambient CLI 0.144.1 | Official release/source inspection at Codex commit `e363b08c9175ac1cbe5893615dd2cb9ddf95043b`; this is a spike pin, not acceptance of ADR-0002 or a production dependency |
| D-012 | 2026-08-09 | accepted for Phase 0 spike | Exercise `mcp==2.0.0` through the low-level `Server` API with exact published schemas plus strict app-owned validation and sanitized bounded results | MCP release/source inspection at commit `6f69a3758ebf2ee55ce050f58b470ce11af71133`; decorator defaults do not meet the strict-schema/error-disclosure boundary |
| D-013 | 2026-08-09 | accepted security stop condition | Do not read, copy, or symlink Codex credentials to reconcile authentication with isolated configuration; existing ChatGPT auth and exclusion of ambient config/MCP/plugins must both be proven | Separate `CODEX_HOME` changes file/keyring auth scope, while the real home recursively inherits ambient configuration and an empty MCP table does not clear it |
| D-014 | 2026-08-09 | accepted security stop condition | Do not treat legacy `sandboxMode: "read-only"` or permission profiles alone as the Codex security boundary; tool inventory and negative-capability sentinels govern acceptance | Tagged 0.146.0 source shows broad legacy reads and no complete SDK tool allowlist; `apply_patch`, `view_image`, ambient MCP and hosted capabilities require executable denial evidence |
| D-015 | 2026-08-09 | accepted, ADR-0004 | Support official Rekordbox XML import/export as the permanent baseline and keep `pyrekordbox`/database/ANLZ paths excluded unless a later immutable-snapshot, version-gated proof is approved | Mandatory source research plus the independently reviewed 11-test synthetic hostile-XML spike; real Rekordbox 7.2.14 export validation remains later manual evidence |
| D-016 | 2026-08-09 | superseded by D-024 | Treat the seven-test generated PCM spike as feasibility evidence only; the original entry kept ADR-0005 Proposed pending a production stack | The spike still proves only bounded local measurement, isolation/cleanup and model-asset rejection; D-024 narrows the accepted ADR to the provider architecture and moves exact stack selection to Phase 3 |
| D-017 | 2026-08-09 | accepted security stop condition | Treat stock TypeScript Codex SDK 0.146.0 as insufficient for same-auth ambient-config isolation until P0-006 proves an explicit narrow adjustment; never copy credentials or silently inherit user MCP/plugins | Direct `codex exec --ignore-user-config --ignore-rules` preserves the real auth home, but the stock SDK cannot pass those flags and cannot expose pre-turn tool inventory; an app-owned argument-injecting wrapper is only a candidate pending sentinel and packaging review |
| D-018 | 2026-08-09 | superseded in status by D-029 | Carry forward a private authenticated Unix-socket core protocol with one SQLite owner, distinct launcher-created `trusted-main`/`mcp-bridge` capabilities, cooperative canonical database/socket ownership locks, and core-owned single-use approval bound to boot and trusted-main app epochs | Independently reviewed 38-test P0-013 spike established the candidate; D-029 records later ADR acceptance after parity, MCP, and broker evidence |
| D-019 | 2026-08-09 | historical stop condition, narrowed by D-022/D-034 | Do not accept the TypeScript 0.146.0 wrapper candidate or advance P0-006/P0-007 while its real lifecycle, capability-denial, same-auth ambient-isolation, and Python MCP transport evidence is absent | D-022 later established local Python transport and D-034 established scoped authenticated new/resume lifecycle. Real cancellation, capability denial, ambient isolation, Codex-to-MCP use, and containment remain blocking, so the original no-advance outcome still holds for narrower reasons |
| D-020 | 2026-08-09 | accepted stop condition, narrowed by D-022/D-034 | Treat the independently reviewed wrapper package as local contract evidence only: it proves validated supervision of the shim and inherited helper process group, not containment or cleanup of a descendant that creates a different process group | Later evidence adds Python transport and scoped authenticated lifecycle, but the escape fixture remains `proof_unavailable`; real cancellation, negative capability, ambient isolation, real MCP-call, and distinct MCP-group cleanup gates remain blocked |
| D-021 | 2026-08-09 | accepted, ADR-0006 | Store versioned normalized little-endian float32 vectors in a validated app-owned SQLite schema and begin with bounded brute-force true cosine behind `EmbeddingIndex`; do not add ANN without the Phase 4 10,000-track benchmark and packaging/license evidence | Independently reviewed 13-test synthetic spike; both initial medium counterexamples were fixed. No embedding model is approved, and the 20,480,000-byte 10k×512 payload calculation is an estimate, not a performance claim |
| D-022 | 2026-08-09 | accepted for Phase 0 transport evidence | Carry forward the independently reviewed Python MCP 2.0 low-level stdio adapter as the local transport candidate, while keeping ADR-0003 and P0-007 blocked on the real Codex call, private core-protocol parity, application composition, packaging, and containment gates | Fresh review reproduced 9/9 warning-strict tests, bounded commands, exact environment inventory, hostile input rejection, protocol-only stdout, and direct-server-PID cancellation cleanup with no high/medium finding; lower diagnostic, timeout, pre-parse byte-cap, lock-hash, and descendant limits remain explicit |
| D-023 | 2026-08-09 | accepted, ADR-0001 | Use Electron + React + strict TypeScript on Electron's embedded Node 24 major line with a bundled CPython 3.12 core; require renderer sandboxing and a superseding security ADR for any exception | Official lifecycle/security evidence, independently reviewed Python/socket topology, named `Mac17,2` measurement plan, and explicit launch/memory/package budgets; exact supported patches and reproducible runtime bundle remain Phase 1 gates |
| D-024 | 2026-08-09 | accepted, ADR-0005; supersedes D-016's status condition | Use a versioned local-only staged analysis architecture with a guaranteed metadata/basic-feature fallback, no approved optional model/provider, Essentia excluded, and integrity-safe assets; select exact decoder/metadata/DSP/codecs only at the Phase 3 gate | Independently reviewed generated-PCM boundary plus direct license/runtime research; accepting the architecture does not promote spike code or select NumPy/SciPy/librosa/soundfile |
| D-025 | 2026-08-09 | accepted, ADR-0007 | Target arm64 direct Developer ID distribution with mandatory renderer sandboxing, runtime-immutable/integrity-verified resources, mutable Application Support state, no ambient helper fallback, and separate unsigned-engineering versus credentialed-release gates | Electron/Apple primary documentation and independent architecture/security review; Codex helper inclusion is conditional on ADR-0002 plus service/auth/redistribution/signing approval |
| D-026 | 2026-08-09 | accepted for the private core wire contract | Forbid JSON floating-point values, use signed-64 integers with Unicode-code-point key ordering, require exact integer protocol version, and encode delays/expiries as bounded integer milliseconds | Python red/green corrections plus the original 14-test TypeScript/Python differential oracle remove the observed float/negative-zero/exponent and boolean-version incompatibilities; full application composition remains a later implementation gate under accepted ADR-0003 |
| D-027 | 2026-08-09 | accepted for the private core wire contract | Encode fractional domain values as schema-named scaled signed-64 integers (`bpm_milli`, unit-interval `*_ppm`, signed `*_signed_ppm`, and `*_ms`), quantized round-half-even from exact decimal input with reject-not-saturate bounds; carry embeddings as typed byte blobs | Architecture re-review exposed the ambiguity after the integer-only correction. ADR-0003 now fixes the cross-boundary representation while Phase 1 still owns concrete DTO generation and round-trip tests |
| D-028 | 2026-08-09 | accepted for local Phase 0 evidence | Carry forward the exact one-server/one-tool Codex registration and coherent MCP-event credit state machine as the integration candidate, without crediting a real service call | Fresh independent review reproduced 43/43 TypeScript tests/typecheck, 9/9 Python warning-strict tests, and the real local stdio probe with no High or Medium local finding; packaged Codex enforcement, real service events, ambient isolation, application composition, and escaped descendants remain blockers |
| D-029 | 2026-08-09 | accepted, ADR-0003; supersedes D-018's status condition | Use one supervised Python core as sole SQLite owner, a private authenticated framed Unix socket for independent main/MCP sessions, and a separate stateless Python MCP stdio bridge with the reviewed integer/scaled-integer wire contract | Independently reviewed 38-test topology/approval proof, 9-test MCP transport, tagged SDK main-broker comparison, and corrected 15-test differential/local codec suite are sufficient to choose the architecture; Electron composition, secure packaged capability delivery, real supervision, and actual Codex-to-MCP use remain later implementation gates |
| D-030 | 2026-08-09 | rejected after independent review | Do not use pinned Codex 0.146.0's built-in `:read-only` profile for this product boundary | Tagged source and an escalated model-free probe show that built-in `:read-only` grants `:root` read access: outside-workspace files remain readable even though writes/network are restricted. Configuration parse success is not workspace isolation |
| D-031 | 2026-08-09 | rejected after independent review | Do not use pinned Codex 0.146.0's custom `:minimal` platform defaults as the DJ data boundary | Tagged macOS source and an escalated exact-profile probe show broad system/shared-temp reads plus shared-temp writes; exact deny entries did not carve those defaults out. The earlier inside/outside fixture missed this baseline and overclaimed workspace-only behavior |
| D-032 | 2026-08-09 | accepted only as fail-closed local Phase 0 evidence | Preserve named `dj_read`, rewrite only the SDK's unparseable filesystem override to a strict inline workspace-read table with no `:minimal`, and block provider startup unless exact config checks and the pinned child-start fail-closed gate pass | The helper reports one exact MCP server/tool, strict mode rejects an unknown canary before service use, and the exact macOS 0.146.0 profile returns 134 before the trusted probe emits output or creates workspace/shared-temp markers. This removes the broader platform defaults but makes sandboxed child execution unavailable; it does not accept ADR-0002 or prove lifecycle, direct tools, MCP, or service behavior |
| D-033 | 2026-08-09 | accepted for local timeout control | Abort the SDK signal only when the total deadline actually wins; after either fulfillment or rejection, clear the deadline and run bounded descendant cleanup without a late signal abort | The first post-permission authenticated run exposed an unhandled ChildProcess `AbortError` when `finally` aborted an already-settled SDK operation. Fulfilled- and rejected-operation regressions now keep the signal unaborted; deadline and ignored-signal regressions still abort and clean up |
| D-034 | 2026-08-09 | accepted as narrow Phase 0 SDK/auth evidence | Credit the corrected `stage=mcp_echo_tool` result only for existing-ChatGPT-auth new/resume streaming and application-validated structured outputs; do not credit a specific completion event, cancellation, MCP, sentinels, ambient isolation, or containment | `real-probe.ts` assigns the stage only after `runLifecycle` returns. Independent read-only review traced both consumed streams, non-null thread ID, exact resume ID, and bounded result validation. The old reported `service` category was a generic fallback, so the exact MCP-stage failure remains unknown. Another identical external run is not justified without material diagnostic, runtime, or containment change |
| D-035 | 2026-08-09 | accepted evidence boundary for the pinned runtime | Treat local stdio MCP placement and cleanup as a distinct process boundary that needs direct evidence; never infer MCP startup or failure from the model-command sandbox preflight | Tagged Codex 0.146.0 source selects `LocalStdioServerLauncher` for local MCP, spawns the configured server directly as an orchestrator child in a new process group, and attempts group termination on transport close/drop. That source explains why the shim's inherited-group audit cannot prove MCP extinction, but it does not replace an authenticated call or executable cleanup proof |
| D-036 | 2026-08-09 | accepted for failure integrity | A mandatory cleanup rejection must cross the redacted boundary as `cleanup` plus `after_success`, `after_error`, or `after_timeout`; it must never be relabeled as an SDK/MCP/service failure | Independent review found `finally` could overwrite all operation outcomes. Red/green generic and MCP regressions now preserve whether the deadline had won while retaining the no-raw-detail rule |
| D-037 | 2026-08-09 | accepted for lifecycle evidence | Credit exact lifecycle values only when application validation matches the explicit expected IDs independently for both new and resumed streams | Supplying an output schema is not universal enforcement. Wrong-known-ID first/resume regressions now fail closed after stream exhaustion, preventing the retained evidence from exceeding application validation |
| D-038 | 2026-08-09 | accepted for promise outcome integrity | Track promise rejection as explicit state, never by testing whether its reason is defined | JavaScript permits `Promise.reject()`/`throw undefined`; red regressions showed the earlier check could resolve a failed MCP operation and create false credit. Explicit failure state preserves reasonless rejection and cleanup classification |
| D-039 | 2026-08-09 | superseded by D-040/D-041; not implemented | Put authenticated MCP lifecycle telemetry on a bounded supervisor-owned private Unix socket outside the model workspace; have the instrumented MCP entrypoint connect before serving stdio, report only allowlisted state transitions, accept only a scoped shutdown command, and let the supervisor verify group extinction | The 2026-08-10 personal-use scope stopped P0-016 before completion. Its motivation remains historical evidence, not a current gate |
| D-040 | 2026-08-10 | accepted; supersedes commercial Phase 0 stop rule | Build every user-facing feature as a personal full-feature MVP using milestone-sized vertical slices, focused verification, and fixes driven by Joe's real use | Approved personal MVP design |
| D-041 | 2026-08-10 | accepted for personal MVP; accepts ADR-0002 | Use the official TypeScript Codex SDK in Electron main under ordinary same-user trust; retain schema validation, ID checks, renderer isolation, and confirmed writes without requiring P0-016 or exhaustive capability-denial proof | Joe already trusts and regularly authorizes Codex on this Mac; historical KI-022/KI-045/KI-049 remain documented limitations |
| D-042 | 2026-08-10 | accepted workflow | Push substantial self-contained green slices and every completed milestone to `origin`; never push broken code merely for cadence or include personal library/audio/app data | Approved design GitHub checkpoint cadence |
| D-043 | 2026-08-10 | accepted for M1 implementation | Pin the desktop workspace to Electron 43.3.0, React 19.2.8, TypeScript 7.0.2, Vite 8.2.1, Zod 4.4.3, and pnpm 11.16.0 under one committed lockfile | M1 production build plus contract, boundary, renderer, and Electron-flow evidence; ADR-0001 implementation update |
| D-044 | 2026-08-10 | accepted development boundary | Run the M1 Python core with `DJ_COPILOT_PYTHON` or host `python3` compatible with Python 3.12+, while keeping the self-contained CPython 3.12 arm64 runtime as an M7 packaging deliverable | M1 is exercised on host CPython 3.14.3; development success does not claim a bundled runtime |
| D-045 | 2026-08-11 | accepted delivery workflow | Defer all visual QA, including native macOS picker interaction and screenshot-based review, until every M1–M7 feature is implemented; milestones may close on focused automated and non-visual integration evidence when no in-scope code defect is known | Joe explicitly requested one final hands-on test period instead of visual gates during implementation; deferred checks remain visible and are not recorded as passes |
| D-046 | 2026-08-11 | accepted for M2 development | Use external FFmpeg/ffprobe 8.1.2 plus pinned NumPy 2.4.4 for a transparent local baseline provider; expose heuristic confidence/limitations, keep optional structure/embedding stages unavailable, and do not bundle the measured GPL-configured Homebrew FFmpeg | Official release/license/package metadata plus local arm64 measurements in `docs/evidence/m2-dependency-selection.md`; M7 owns a separately reviewed distributable decoder |
| D-047 | 2026-08-11 | accepted for M2 implementation | Persist one latest versioned analysis job and feature result per stable track in schema-v2 app SQLite, use one resumable local worker, preserve queued/paused work across core restarts, invalidate analysis when imported source path/availability changes, and return only explicitly requested status items plus global counts | M2 repository/manager/service/renderer tests; pre-DDL M1 database backup; exact running fingerprint completion; provider/pipeline cache keys; path-free typed renderer boundary |
| D-048 | 2026-08-11 | accepted and implemented in M3 | Keep discovery on schema-v2 metadata and successful local features with bounded casefolded token search, brute-force deterministic `feature-similarity-v1`/`transition-v1`, all eight fixed intents, and explicit confidence/missing evidence; do not add FTS, fuzzy matching, embeddings, a model, ANN, or a 10k gate until representative use shows a need | Implemented at `5a5d59d`, `bb85aaa`, and reviewed closure `1e9d347`; 17 deterministic ranking tests, eight repository/service tests, strict desktop contracts, eight renderer behaviors, the generated-fixture Electron flow, and independent READY disposition are green; saved filters remain assigned to M5 |
| D-049 | 2026-08-11 | accepted and implemented in M4 | Build the personal set workflow from bounded full JSON snapshots in exactly three schema-v3 draft tables; reuse `transition-v1` for generation, alternatives, optimization, and inspection; export one deterministic self-contained official XML only after a native-picker preview and main-owned single-use confirmation | Implemented at `3f87261`, `abad8c3`, `82bbf94`, and `dd741d6`; post-review `pnpm verify:m4` passed 119 core, 116 desktop, strict typecheck/build, and five Electron flows, and corrections closed at `10d2511` without event sourcing, a second scoring stack, drag/drop/chart dependencies, Rekordbox database access, XML merging, or renderer filesystem paths |
| D-050 | 2026-08-11 | accepted and implemented in M5 | Add exactly three schema-v4 app tables for track metadata, saved filters, and strict feedback; use deterministic `preference-linear-v1` with a five-signal activation threshold and 150,000-ppm cap; preserve tags/notes/filters on reset while clearing ratings/feedback; compare one baseline candidate universe and export only a confirmed bounded profile JSON | Implemented through `b5cbee6` plus the reviewed closure correction; post-fix `pnpm verify:m5` passes 151 core, 137 desktop, strict typecheck/build, and six Electron flows. The single reviewer found one stale visible comparison after reset; a RED/GREEN renderer regression now invalidates and refetches it without adding ML, embeddings, FTS, a background learner, an event framework, or a new runtime |
| D-051 | 2026-08-11 | accepted and implemented in M6 | Pin official `@openai/codex-sdk@0.147.0` behind `AIProvider` in Electron main, reuse only existing ChatGPT/Codex authentication, leave the model unset, and implement search/plan/one-revision/explanation through bounded context plus existing core commands with zero production MCP tools | Post-review `pnpm verify:m6` passes 151 core, 219 desktop, strict typecheck/build, and seven generated Electron flows; the separate real smoke proves exact SDK/auth, structured search/plan, same-thread resume, cancellation, and explicit renderer status. Three review defects received RED/GREEN fixes; reviewer returned READY. Development-only MCP types resolve the published SDK declaration without adding a production MCP server |

## Current decision state

No architectural decision remains blocked by the superseded commercial Phase 0 gate. Production implementation still requires the focused milestone evidence named in the accepted ADRs.

See `docs/adr/README.md` for status and evidence requirements.
