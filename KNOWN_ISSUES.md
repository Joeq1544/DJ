# DJ Copilot Known Issues

Last updated: 2026-08-11

An M2 development runtime now exists. No reproducible normal-workflow M1/M2 product defect is open at the implementation checkpoint; this ledger retains resolved development/spike defects, developer-tooling issues, and explicitly accepted personal-use limitations. General research uncertainty and not-yet-run personal-input checks remain in `TASKS.md` or the milestone evidence.

| ID | Status | Severity | Reproduction | Root/suspected cause | Workaround or fix | Owner | Regression test |
| --- | --- | --- | --- | --- | --- | --- | --- |
| KI-001 | open | low, documentation | Request `DJ_COPILOT_BUILD_PROMPT.md` at repository root; only `dj-copilot-codex-master-prompt.md` exists | Specification was supplied under a different filename | Use the discovered file as recorded by D-001; do not rename the user-owned untracked file | primary | Not applicable; checked by repository inventory |
| KI-002 | resolved in Phase 0 worktree | medium, developer tooling | With `[agents] max_concurrent_threads_per_session = 4`, run `codex features list` from the repository; CLI 0.144.1 exits 1 with `expected struct AgentRoleToml` | Current published manual schema is newer than the installed CLI's accepted legacy field | Use verified `agents.max_threads = 4` and reassess after CLI upgrade | primary | `test_installed_codex_cli_loads_the_project_configuration` |
| KI-003 | resolved in Phase 0 worktree | low, evidence accuracy | Initial environment collector records Codex startup warning as its version and an `mdls` error as Rekordbox version | Blind first-line parsing; Spotlight metadata lookup does not resolve the installed Rekordbox bundle although its plist exists | Filter startup warnings and read `CFBundleShortVersionString` from the bundle plist | primary | `test_extracts_versions_without_recording_startup_warnings_or_app_paths` |
| KI-004 | resolved in Phase 0 worktree | medium, parser resource bound | Parse three playlist nodes that all reference one track with `max_playlists=2`; the initial spike accepted them | The limit checked the number of track-membership dictionary keys, not actual playlist nodes | Count `Type="1"` nodes before constructing membership records | Rekordbox specialist | `test_limits_playlist_nodes_when_multiple_playlists_share_one_track` |
| KI-005 | resolved in Phase 0 worktree | high, XML entity expansion | Encode an internal-entity document as BOM UTF-16 or explicitly declared no-BOM UTF-16LE; the initial ASCII-byte declaration scan was bypassed and ElementTree expanded the entity | Declaration denial assumed ASCII-compatible bytes before allowing ElementTree to parse encoding-aware input | Restrict the synthetic spike to valid UTF-8/UTF-8-BOM, reject decoded NULs, and scan decoded text for DTD/entity declarations before parsing | Rekordbox specialist + primary | `test_rejects_bom_marked_utf16_dtd_and_entity_before_xml_parse`; `test_rejects_no_bom_utf16le_dtd_and_entity_before_xml_parse` |
| KI-006 | resolved in Phase 0 worktree | medium, test/process reliability | A 50 ms batch timeout marked a valid generated WAV as timed out during Python `spawn` startup | One wall clock incorrectly combined worker startup and analysis time | Require a separately bounded worker-ready handshake before starting the per-file analysis timeout; always terminate/join/close in `finally` | audio-MIR specialist | `test_batch_isolates_corrupt_and_timed_out_files_and_cleans_children` |
| KI-007 | resolved in Phase 0 worktree | medium, evidence accuracy | Feed at least nine irregular synthetic onsets; the initial analysis reported confidence 1.0 | Confidence depended only on interval count, not timing regularity | Multiply evidence count by an exposed mean-deviation interval-regularity component | audio-MIR specialist + primary | `test_irregular_onsets_have_lower_confidence_than_repeated_clicks` |
| KI-008 | resolved in Phase 0 worktree | low, bounded-memory contract | Generate the 16-second click fixture; the initial generator allocated a 768,000-element Python list | Generation wrote chunks but materialized the full signal first | Yield bounded click/silence chunks directly while preserving exact WAV bytes/hash | audio-MIR specialist + primary | `test_generator_exposes_bounded_chunk_iterators`; deterministic click SHA-256 assertion |
| KI-009 | resolved in Phase 0 worktree | high, authorization | Connect as the current UID and self-assert `trusted-main`; the initial topology granted proposal decision authority | Peer UID authenticated only the local account, not the client role | Require distinct launcher-created bounded capabilities for `trusted-main` and `mcp-bridge`; reject missing, cross-role, malformed, or non-ASCII tokens without echo | process-topology specialist + primary | `test_role_capabilities_reject_missing_wrong_and_cross_role_claims`; approval role tests |
| KI-010 | resolved in Phase 0 worktree | high, data ownership | Start a second core against the same database or race a different database against the same stale/live socket | The first spike observed one opener but had no canonical exclusive database/socket ownership or ownership-aware cleanup | Acquire database and socket-path `flock` values before SQLite/bind; probe and recheck socket type/inode; clean only the owned inode; release safely on failed startup | process-topology specialist + primary | `test_second_core_cannot_take_database_or_live_socket`; socket-lock/symlink/start-failure tests |
| KI-011 | resolved in Phase 0 worktree | medium, parser/resource bound | Submit an unbounded stdio line, lone surrogate, deep/node-heavy JSON, or 5,000-digit integer | Model-facing and core decoders lacked a shared complete resource grammar; Python conversion exceptions escaped stable protocol handling | Add byte, duplicate-key, finite-number, signed-64-bit lexical, UTF-8/Unicode, depth, node, and schema bounds on both paths | process-topology specialist + primary | protocol invalid-payload table; `test_stdio_is_byte_bounded_and_uses_strict_json` |
| KI-012 | resolved in Phase 0 worktree | medium, lifecycle/reliability | Disconnect and immediately reconnect a session, stop during handler cleanup, or complete at a cancellation boundary; the initial run intermittently passed only 22/23 and leaked a rejected socket | Session registration/threads/request history and terminal/slot cleanup did not have complete ownership and ordering | Bound sessions/history/lifetimes, own and join handlers, close rejected clients, probe stale disconnected sessions, and release task/capacity before terminal publication | process-topology specialist + primary | reconnect/handshake tests; `test_stop_waits_for_session_thread_and_closed_session_cannot_dispatch`; `test_cancel_at_result_boundary_has_one_terminal_and_recovers_slot` |
| KI-013 | resolved in Phase 0 worktree | medium, approval authorization | Approve a proposal, reconnect trusted main while core stays alive, then execute using the prior approval | Proposal invalidation used only the core boot epoch | Rotate a core-owned app epoch on each authenticated trusted-main connection and bind proposal creation/execution to it | process-topology specialist + primary | `test_trusted_main_restart_invalidates_prior_approval_epoch` |
| KI-014 | resolved in Phase 0 worktree | medium, transaction integrity | Fault an idempotency-record or proposal-use write after inserting the harmless row | Multi-statement SQLite paths did not explicitly roll back partial work | Wrap mutations in explicit commit/rollback scopes and retain retryable state after injected failure | process-topology specialist + primary | `test_failed_idempotency_write_rolls_back_harmless_row`; `test_failed_execute_rolls_back_insert_and_leaves_proposal_retryable` |
| KI-015 | resolved in Phase 0 worktree | medium, timeout | Send a handshake byte or progress frame faster than the socket timeout; the bridge could run indefinitely | `socket.settimeout` bounded inactivity per call, so each incoming frame reset the effective wait | Use one monotonic absolute deadline for connect/handshake and one per request, recompute remaining time around all I/O/pending frames, and close a failed exchange | process-topology specialist + primary | `test_mcp_bridge_partial_handshake_drip_cannot_extend_absolute_deadline`; `test_mcp_bridge_progress_drip_cannot_extend_request_deadline` |
| KI-016 | resolved in Phase 0 worktree | high, process safety | Corrupt or replace the real-probe PID JSONL in its model-readable workspace, or record `childPid: null`, then enter cleanup | Cleanup trusted unvalidated, mutable PID telemetry; on POSIX `-null` became process-group 0 and arbitrary/stale values could target unrelated same-user processes | Use exclusive, no-follow supervisor-owned control storage outside model roots; enforce exact run/parent/invocation/PID/state records; fail closed and never signal invalid, completed, unrelated, stale, or workspace-decoy records | codex-mcp specialist + primary | Supervisor creation/state/malformed/unrelated/reused-PID regression matrix; independently re-reviewed as resolved |
| KI-017 | resolved in Phase 0 worktree | medium, descendant cleanup | Let the direct helper exit while its inherited process group remains alive, make a signal/probe fail, or leave a distinct shim stuck during the active audit | Termination initially settled on the direct child, so completed telemetry and a zero-survivor result could precede verified group extinction | Require direct-child settlement and both shim/helper-process-group extinction under one absolute deadline; accept group extinction only when `kill(-pgid, 0)` returns `ESRCH`; fail closed on every other probe/signal result | codex-mcp specialist + primary | No-op/failed-signal, group-ESRCH ordering, distinct stuck-shim, cooperative-grandchild, supervisor-completion, and scoped-survivor tests; final independent review approved 36/36 |
| KI-018 | resolved in Phase 0 worktree | medium, security evidence | Return all `false` from the negative-capability turn after a simulated successful direct file/image/audio read | Model self-report and the public SDK event union cannot prove that unobservable reads were denied | Ignore model booleans for denial evidence; count only observable events/markers and retain every unobservable path as `proof_unavailable` blocker | codex-mcp specialist + primary | `negative-capability classification never treats model booleans as denial proof`; independently re-reviewed as resolved |
| KI-019 | resolved in Phase 0 worktree | medium, lifecycle evidence | Exercise the official SDK start path while testing resume only through a fake lifecycle object | Official-SDK argv capture did not prove resume-ID binding or integrated cancellation/cleanup | Capture official-SDK fake-executable start and exact original-ID resume argv; reject changed/malformed IDs; integrate AbortSignal cleanup; label evidence fake-only | codex-mcp specialist + primary | Official SDK start/resume and AbortSignal tests; independently re-reviewed as resolved |
| KI-020 | resolved in Phase 0 worktree | medium, MCP contract | Inspect `ECHO_LIBRARY_IDS_DEFINITION` after returning `structuredContent` | The contract defined a closed input schema but no closed MCP output schema | Publish and enforce an exact closed output schema with known-ID/count bounds and fail closed on extra/unknown/oversized/content-disagreement results | codex-mcp specialist + primary | Five MCP contract/result tests; independently re-reviewed as resolved |
| KI-021 | resolved in part; remaining work split to KI-049/P0-006 | high, feasibility | Run the exact packaged Codex 0.146.0 helper through the isolated SDK wrapper with existing ChatGPT auth | Early runs failed before lifecycle. The corrected run reached `mcp_echo_tool`, which can occur only after new and resumed streamed turns publish/consume validated structured results. It then failed before MCP credit or sentinels | Credit only the traced lifecycle facts; keep MCP, cancellation, sentinels, ambient isolation, and containment blocked | primary + codex-mcp specialist | Authenticated new/resume lifecycle evidence independently classified; exact MCP-stage failure remains KI-049 |
| KI-022 | accepted personal-use limitation; non-blocking | high under commercial threat model, process containment | Have a helper descendant create a different process group before the wrapper begins cleanup | POSIX process-group supervision cannot discover or signal a descendant after it escapes the supervised group; the current wrapper deliberately reports `proof_unavailable` rather than a false-clean result | Preserve the fixture and evidence; revisit for public distribution, observed survivor behavior, or a future stronger trust boundary | primary + codex-mcp specialist | Escape fixture remains `proof_unavailable`; this is accepted for ordinary same-user personal use, not relabeled as a pass |
| KI-023 | resolved in Phase 0 worktree | medium, ranking correctness | Store approximate-unit vectors accepted at tolerance 0.1 and search them using raw dot product | Normalization tolerance was incorrectly reused as score epsilon; raw dot products exceeded 1 and could rank true cosine 0.48 above 0.50 | Divide a stable dot product by both observed norms, bound only floating excursions, and use a fixed tiny endpoint epsilon independent of storage tolerance | primary + independent reviewer | `test_cosine_divides_by_observed_norms_within_declared_tolerance`; fresh 13-test re-review PASS |
| KI-024 | resolved in Phase 0 worktree | medium, migration integrity | Pre-create `track_embeddings` without STRICT/check constraints, then call `migrate()` | `CREATE TABLE IF NOT EXISTS` treated any same-named table as the accepted schema; DDL failure could also leave a partial table | Record schema version 1, validate exact STRICT table/index SQL, reject unknown/weaker schemas, and use explicit transactional fresh DDL with rollback | primary + independent reviewer | Weaker-schema, fresh-version/STRICT, failed-DDL rollback, and exact backup/version tests; fresh re-review PASS |
| KI-025 | open | low, diagnostic disclosure | Invoke the standalone Python MCP probe with a nonexistent server path | The SDK child has no separate `errlog`, so startup diagnostics can reveal local interpreter/server paths before the probe's fixed failure line | Do not expose the spike probe to model or remote output; production composition must capture/redact child diagnostics and enforce an overall timeout | primary | Fresh Python MCP review reproduced the path disclosure; production regression belongs to Phase 1/5 |
| KI-026 | resolved in Phase 0 worktree | medium, evaluation false-clean | Hide a fabricated track ID beyond the rubric's depth/node traversal bound | Bounded scanning stopped silently and treated an incomplete scan as zero unknown IDs | Carry an explicit completeness flag and fail the ID rubric whenever traversal truncates | primary | `an ID traversal that exceeds its bound fails closed` |
| KI-027 | resolved in Phase 0 worktree | medium, report confinement | Pre-create `evaluation.json` as a symlink, then write evaluation reports | Ordinary `writeFile` followed the target and overwrote it | Require both report paths to be absent and create them with exclusive `wx` semantics | primary | `reports refuse an existing or symlinked output file without changing its target` |
| KI-028 | resolved in Phase 0 worktree | medium, diagnostic disclosure | Run the README's package-manager mock command with a private output directory | The package runner printed the full invocation, including the output path, before the application emitted its stable message | Document and execute the tested `pnpm --silent` form; assert stdout contains only the fixed completion line | primary | `the documented silent mock CLI writes without logging the output path` |
| KI-029 | resolved in Phase 0 worktree | medium, evaluation false-clean/data minimization | Put an ID after the per-string cap, use an arbitrary secret under an ID-shaped key, or place more nodes than the scan budget | String truncation did not mark the scan incomplete, ID-shaped values were retained without grammar validation, and wide arrays continued after exhaustion | Fail closed on string/depth/node truncation, stop at the bound, validate exact track-ID grammar, and persist only grammar-valid track IDs | primary | Long-string and wide-traversal fail-closed regressions |
| KI-030 | resolved in Phase 0 worktree | medium, contract validation | Delete a required nested fixture constraint or add a response top-level property | Fixture validation covered only container types and response validation allowed undeclared top-level fields | Validate closed task, constraint, track-fact, response, output, tool-call, and tool-argument shapes with bounded values before grading | primary | `fixtures require complete nested constraints and responses reject extra top-level fields` |
| KI-031 | resolved in Phase 0 worktree | medium, prompt injection evaluation | Place a metadata canary in a model-authored tool argument | Injection grading inspected only assistant text and structured output and was also skipped when schema validation failed | Independently scan the entire bounded response graph, including keys and tool arguments; fail closed on incomplete traversal | primary | `an injection canary hidden in tool arguments fails injection resistance` |
| KI-032 | resolved in Phase 0 worktree | medium, plan consistency | Exclude a selected track or overlap/overrun SetPlan segments | Output shape checks did not reconcile selection arrays and accepted individually valid but globally incoherent segment ranges | Cross-check selected, required, and excluded IDs; require segments to be contiguous from zero through the declared duration | primary | `a SetPlan cannot exclude a selected ID or publish incoherent segments` |
| KI-033 | resolved in Phase 0 worktree | medium, evaluation resource bound | Supply a 10,000-property data/getter object beneath a response field | `Object.entries` read every value before the node-budget loop, and a later correction still iterated injection descriptors after exhaustion | Traverse own enumerable data-property descriptors incrementally, reject accessors/holes without invoking them, and check the remaining budget before every descriptor read | primary | Accessor-backed and ordinary-wide-object bounded traversal regressions |
| KI-034 | resolved in Phase 0 worktree | medium, evaluation false-clean | Label a fixture `injection`, `cancellation`, `explanation`, or another behavior category while deleting its category-specific stimulus/assertion fields | Generic shape checks did not bind category names to required evidence | Require behavior-specific fields and mock outcome semantics for every non-shape category before accepting the corpus | primary | `fixture categories require their behavior-specific stimuli and assertions` |
| KI-035 | resolved in Phase 0 worktree | medium, evaluation crash/resource bound | Put throwing getters in an oversized array under an ID-shaped key such as `trackIds` | The scanner's `slice(0, limit)` invoked accessors before validating the array | Inspect only bounded indexed data-property descriptors, reject holes/accessors without invoking them, and fail the ID check | primary | `ID-shaped arrays reject accessors without invoking or throwing from them` |
| KI-036 | resolved in Phase 0 worktree | high, protocol interoperability | Canonicalize/frame `1.0`, `-0.0`, or exponent-form JSON in Python and TypeScript | Python preserved float kinds/spellings that JavaScript `number` cannot reproduce safely for canonical hashes and framing | Forbid all wire floats, use signed-64 integers/TypeScript `bigint`, and express delays/expiries as bounded integer milliseconds | primary + core-protocol parity specialist | Python float red/green cases and TypeScript/Python `float_contract_unsupported` differential tests |
| KI-037 | resolved in Phase 0 worktree | medium, protocol schema | Send JSON `true` as handshake version 1 | Python `bool` subclasses `int`, so equality accepted it while TypeScript rejected it | Require exact Python `int` type and numeric value 1; compare stable rejection with the Python oracle | primary + core-protocol parity specialist | Boolean-version parity regression in both suites |
| KI-038 | resolved in Phase 0 worktree | medium, process reliability | Run the integrated gate under enough scheduler load that a killed shim is not reaped within the audit's 100 ms post-KILL window | The bounded audit allowed too little time for Node to observe child exit even though scoped termination was progressing | Retain the absolute bound but allow 1,000 ms for post-KILL reaping; measure the active path and require completion within two seconds | primary | Active-audit timing assertion; corrected escalated root gate passed |
| KI-039 | resolved in Phase 0 worktree | medium, protocol integrity | Put `new Array(5000)` in a TypeScript request payload | `Array.map().join()` skipped sparse slots, bypassed node accounting, emitted invalid `[,,,,]` JSON, and produced a frame the same decoder rejected | Preflight the remaining node budget, iterate indexed own data descriptors, and reject holes/accessors without invoking them | primary + architecture reviewer | `canonical input rejects sparse and accessor-backed arrays before framing`; TypeScript suite 15/15 |
| KI-040 | resolved in Phase 0 worktree | medium, MCP evidence accuracy | Emit a distinct expected-tool `item.started` event, then one valid completed call and assistant result | MCP credit counted only completed events, so an incomplete duplicate dispatch was ignored | Track one coherent MCP call ID and state across exact started/updated/completed envelopes; validate arguments in every phase and reject duplicates or invalid transitions | primary + codex-mcp reviewer | Expanded `required MCP echo rejects failed or duplicate calls and always cleans up`; full suite 43/43 |
| KI-041 | resolved in Phase 0 worktree | high, configuration/feasibility | Parse the exact SDK-generated custom `dj_read` filesystem profile with the matching packaged Codex 0.146.0 helper | SDK object flattening emitted the scoped `:workspace_roots`/`.` table as repeated dotted overrides; the runtime rejected it as `FilesystemPermissionToml`, so the real run stopped before lifecycle/MCP/sentinels | Keep the SDK input exact, but have the validated shim rewrite only that entry to one strict inline workspace-read filesystem table; require matching-helper parsing before provider use | primary + codex-mcp specialist | Red actual-helper regression failed before the fix and passes after it; current local TypeScript suite 47/47 plus typecheck |
| KI-042 | resolved in Phase 0 worktree | high, filesystem confidentiality | Select built-in `:read-only`, then read a synthetic file or `/etc/hosts` outside the workspace through `codex sandbox` | Pinned source maps the built-in profile to root-wide read; writes and network are restricted, but personal files, secrets, and raw audio would remain readable to direct read capabilities | Reject D-030 and retain a named custom profile | primary + codex-mcp reviewer | Focused regression failed on built-in; the later `:minimal` alternative was separately rejected by KI-044 |
| KI-043 | resolved in Phase 0 worktree | medium, configuration integrity | Add an unknown feature override while the old `mcp list` preflight checks only exit 0 | Non-strict `mcp list` ignored the key and did not validate its output, so a misspelled security control could appear accepted | Inject `--strict-config` into every real exec; require an unknown-key canary to fail before service use; parse exact `mcp get --json` server/transport/tool/timeouts rather than accepting exit status alone | primary + codex-mcp reviewer | Exact config and strict-canary regression plus shim argv assertion; current suite 47/47 |
| KI-044 | resolved in Phase 0 worktree | medium, filesystem confidentiality/integrity | Run the exact custom `:minimal` + workspace-read profile, then read `/etc/hosts` or a synthetic `/tmp` file and create a synthetic `/tmp` marker | Pinned macOS platform defaults grant broad reads under system/config/application/shared-temp paths and full writes under shared temp. Exact-path deny entries do not carve these raw platform-default rules out in the exercised runtime | Reject D-031 and remove `:minimal`; retain the representative system/shared-temp regression and never describe `:minimal` as workspace-only | primary + codex-mcp reviewer | Red regression observed system/shared-temp reads and shared-temp write; no-`:minimal` focused fail-closed test passes |
| KI-045 | accepted personal-use limitation; non-blocking | feasibility/security | Run any sandboxed child under exact macOS Codex 0.146.0 `dj_read` with workspace read but no `:minimal` | The helper returns exit 134 before even a trusted model-free probe can emit output; adding `:minimal` restores execution only by reopening KI-044's broad platform defaults | Preserve exact-runtime evidence; revisit when implementing M6 if the pinned production flow fails in ordinary use, or before public distribution/stronger isolation | primary + codex-mcp specialist | Exact helper regression remains fail-closed evidence, not a capability pass; the personal MVP accepts normal Codex same-user capabilities |
| KI-046 | resolved in Phase 0 worktree | medium, process reliability/evidence loss | Let an SDK-backed operation settle, then unconditionally abort its signal from `withTotalTimeout` cleanup | Node's child-process signal listener emitted an unhandled `AbortError`; the authenticated probe terminated without its redacted result after entering the MCP stage | Abort only from the deadline callback. Fulfilled or rejected operations clear the timer and run unconditional scoped cleanup without a late abort | primary + codex-mcp reviewer | Success regression failed before the first fix; reviewer then reproduced rejection-path failure; both settled-signal regressions plus three timeout/error cases pass, full local suite 47/47 |
| KI-047 | resolved in Phase 0 worktree | low, test reliability | Start the cooperative-parent cleanup fixture with only a 250 ms synthetic helper timeout | On the exercised host, the helper could be killed before writing its PID fixture, so the test failed setup rather than checking cleanup | Increase only the test's startup/timeout window to 1,000 ms; leave production cleanup deadlines and assertions unchanged | primary | Isolated red reproduced missing PID setup; isolated green and full 47-test suite pass |
| KI-048 | resolved in Phase 0 worktree | medium, evidence integrity | Classify an unmatched redacted provider error such as `required Codex MCP echo failed` | `classifySanitizedFailure` defaulted every unmatched error to `service`, so the corrected authenticated result could be misread as evidence of an upstream outage even though `runRequiredMcpEcho` had erased the cause | Reserve `service` for messages that explicitly identify service/upstream failure; classify opaque errors as `unknown`; emit only allowlisted MCP-stage reasons for future runs | primary + codex-mcp reviewer | Red generic-error and stage-reason cases failed first; current full package 52/52 plus typecheck and root gate 186/186 plus three typechecks pass |
| KI-049 | accepted personal-use limitation; non-blocking | high under original feasibility gate, evidence | Complete the corrected authenticated probe after `runLifecycle` returns and `runRequiredMcpEcho` begins | The run exited 1 with `required_python_mcp_echo_not_observed`; its old `{category:"service"}` output was only the generic fallback. No discovery/startup/tool event or sentinel was credited, so the exact failure is unavailable | Preserve the failed evidence; M6 performs one focused real existing-auth workflow and fixes the concrete integration path if it fails, without reviving P0-016 | primary + codex-mcp specialist | Full 52-test historical package/typecheck and 186-test root gate were green; external MCP and cleanup evidence remain absent and are not relabeled as passed |
| KI-050 | resolved in Phase 0 worktree | medium, cleanup/evidence integrity | Let `withTotalTimeout` settle, then make its mandatory cleanup callback reject | The `finally` rejection overwrote success, operation failure, or `total timeout`; `runRequiredMcpEcho` then mislabeled it `sdk_stream_error` | Track the operation outcome separately; emit only `cleanup` with `after_success`, `after_error`, or `after_timeout`; preserve a successful cleanup's original result/failure | primary + codex-mcp reviewer | Red cleanup-after-success/error/timeout cases across generic and MCP callers; 52/52 plus typecheck and 186/186 root pass |
| KI-051 | resolved in Phase 0 worktree | medium, lifecycle evidence integrity | Return a different but globally known fixture ID from the new or resumed lifecycle stream | `runLifecycle` supplied an exact output schema but its application validator accepted any 1–5 known fixture IDs and did not compare the two results, so retained evidence overstated the exact value | Pass the exact expected IDs into `runLifecycle`; reject either new or resumed output unless its validated IDs match exactly | primary + codex-mcp reviewer | Wrong-known first and wrong-known resume regressions failed before correction; 52/52 plus typecheck and 186/186 root pass. This protects future runs and does not expand historical credit |
| KI-052 | resolved in Phase 0 worktree | medium, MCP false credit | Reject an operation with JavaScript `undefined` and let cleanup succeed | The cleanup fix used `failure !== undefined` as its failure flag, so a reasonless rejection resolved as success; `runRequiredMcpEcho` could return `undefined` and the caller could mark MCP observed | Track a separate boolean/tagged rejection state independent of the rejection value | primary + codex-mcp reviewer | Generic `Promise.reject()` and MCP stream reasonless-rejection regressions failed before correction; rejecting cleanup remains `after_error`; 52/52 plus typecheck and 186/186 root pass |
| KI-053 | resolved in M0 | medium, build integrity | Run `pnpm typecheck` in `spikes/codex-mcp` after the interrupted P0-016 rewrite | Ten TypeScript errors remained because `mcp-control.test.ts` targeted an abandoned supervisor API and production registration referenced missing `observed_server.py` | Restored direct reviewed `server.py` registration and removed only the four unfinished P0-016 control-channel files | primary | Red direct-registration expectations failed 9/46 before the correction; 46/46 targeted and 52/52 full tests pass outside the outer workspace sandbox; typecheck passes; no P0-016 runtime references remain |
| KI-054 | open, non-blocking | low, developer tooling | Run `pnpm --dir app/desktop test -- analysis-screen library-screen` with pnpm 11.16.0 | pnpm forwards the separator as `vitest run -- ...`; the focused process stalls instead of exiting after the tests | Omit the separator (`pnpm --dir app/desktop test analysis-screen library-screen`) or invoke explicit Vitest paths; aggregate `pnpm test:desktop` is unaffected | primary | Equivalent explicit Vitest run passes 24/24; package-script form without the separator also passes 24/24 |
| KI-055 | resolved in M2 | low, verification type integrity | Run the first complete `pnpm verify:m2` after adding the generated-audio Electron flow | Playwright transpiled the E2E file, but strict TypeScript inferred its homogeneous fixture rows as variable-length `string[]`; with `noUncheckedIndexedAccess`, destructured `filename` was `string \| undefined` | Mark the literal fixture matrix `as const` so every row is an immutable three-item tuple | primary | First aggregate passed 51 core and 62 desktop tests then failed the exact typecheck at `analysis-flow.spec.ts:85`; focused typecheck and both the 51-test pre-review and 56-test post-review aggregates passed after correction |
| KI-056 | resolved in M2 review | medium, analysis correctness | Start analysis, then reimport the same external track with a changed path or remove it before the claimed worker completes | Analysis runs outside the repository lock; import retained a stable running job, and success inserted `track_features` before checking that the track/job still matched | Invalidate jobs/features when normalized path or availability changes; accept completion only for an existing track with a running job and exact recorded fingerprint | primary + M2 reviewer | Three race regressions failed before correction, then passed; repository 17/17, manager 7/7, and post-review aggregate 56/56 core passed |
| KI-057 | resolved in M2 review | medium, degraded startup | Launch the core under `python -S` against an existing library database | `provider.py` imported NumPy and built profiles at module import, so missing NumPy exited the process before capability degradation | Guard the optional import/profile initialization and return an unavailable provider before analysis work; keep service reason generic/path-free | primary + M2 reviewer | Two subprocess regressions failed at import before correction, then passed; provider + analysis service passed 16/16 twice; post-review aggregate 56/56 core passed |
| KI-058 | resolved in M2 review | low, recovery documentation | Follow the pre-M2 backup filename in the draft M2 recovery guide | The guide appended `.pre-m2` to the full database filename, but implementation replaces the original suffix using `Path.stem` | Document `dj-copilot.pre-m2.sqlite3`, followed by numbered siblings | primary + M2 reviewer | Migration test expects the exact first and numbered sibling names; recovery/evidence now match implementation |
| KI-059 | open, non-blocking | low, unused renderer precision | Inspect `mtimeNs` in the strict desktop analysis DTO for a real epoch-nanosecond value | JSON parses the core integer into a JavaScript `number`, whose safe-integer range is smaller than current nanosecond timestamps | Keep cache identity authoritative in the Python fingerprint, as implemented; change the unused renderer field to a decimal string before it becomes behaviorally significant | M3+ shared-contract owner | Reviewer confirmed the renderer does not use `mtimeNs` for cache identity; provider/core fingerprint tests remain exact |
| KI-060 | open, non-blocking | low, prerequisite version parsing | Supply a hypothetical FFmpeg version line beginning `ffmpeg version 8.1.20` | Development capability validation uses `startswith("ffmpeg version 8.1.2")` rather than parsing the complete version token | The measured executable is exactly 8.1.2; parse the first token exactly when decoder version policy is revisited, no later than M7 packaging | M7 packaging owner | Current executable/version evidence is exact 8.1.2; mismatch tests cover other version prefixes but not this hypothetical longer token |
| KI-061 | resolved in M3 | low, integration-harness startup order | Launch the M3 Electron flow and poll the main-process test hook before waiting for the first renderer window | The hook is installed during main-window creation, so the first focused run attempted `getStatus` on an absent hook and failed before exercising discovery | Wait for `firstWindow()` before polling core readiness; retain the production flow and assertions unchanged | primary | Initial focused flow failed at the readiness probe; corrected `test:e2e:m3` passed 1/1 and the aggregate passed all four Electron flows |
| KI-062 | resolved in M3 review | medium, playlist browsing correctness | Import a playlist containing the same track in positions `(A, B, A)`, then browse it through M3 `list_tracks` with empty or active filters | The joined M3 evidence projection grouped only by track ID, so list/search collapsed the repeated position even though legacy playlist browsing preserved it | Group list/search evidence by track ID plus selected position, allow repeated IDs only in the filter path, and keep discovery catalogs uniquely grouped by track ID and ordered by first occurrence | primary + M3 reviewer | Two regressions failed before correction and now cover cursor paging plus the path-free wire response; focused discovery/service 25/25 and post-review aggregate core 81/81 pass; reviewer independently rechecked the focused suite |
| KI-063 | resolved in M4 | medium, Rekordbox compatibility | Import an official playlist node with numeric node-level `KeyType="0"` and child `<TRACK Key="1"/>` references | The production importer accepted only the older generated-fixture child-level textual `KeyType="TrackID"`/`"Location"` form and rejected the official numeric form as `unsupported_playlist_reference` | Accept either internally consistent official numeric or legacy textual reference form; reject conflicts/mixing; validate self-contained exports with the production parser before atomic replacement | Rekordbox specialist | Red official-form reproduction preceded the correction; 19/19 focused parser/writer tests pass at `3f87261`, including mixed/conflicting forms, deterministic reparse, source/destination preservation, races, symlinks, and cleanup |
| KI-064 | resolved in M4 integration | medium, set creation | Select Energy or Variety in the first set-launcher implementation and create a draft | Those renderer-only option values were not among the eight strict discovery intents, so the trusted boundary rejected an apparently valid UI action | Render the exact shared eight-intent vocabulary in both launcher and workspace; expose target duration and artist-repeat constraints during creation | primary | Focused renderer test first failed on the four-value menu, then passed 3/3 with exact options and generated constraint payload; complete desktop 115/115 and Electron M4 flow pass at `dd741d6` |
| KI-065 | resolved in M4 integration | medium, saved-version identity | Save the current draft revision, then explicitly view that saved version before making another edit | The service inferred version-view mode only when content revision differed from the live head, so an explicitly requested saved head looked editable and `viewingVersion` was null | Preserve whether `get_set_draft` received an explicit revision and map a matching saved version even when it equals the current head; ordinary live reads remain editable | primary | Core regression first returned `None` instead of version 1 and the expanded Electron flow could not enter read-only mode; focused service 5/5, renderer 3/3, and `pnpm verify:m4` pass after correction at `dd741d6` |
| KI-066 | resolved in M4 review | medium, historical draft correctness | Edit numeric goals, view an older saved revision, then use Cmd-Z or prepare an export | Historical mode disabled visible editor buttons but uncontrolled goal inputs retained later values, the global shortcut still mutated the head, and an already prepared export could still be confirmed | Remount numeric goal inputs by content revision, block shortcuts in historical mode, disable prepare/confirm, and clear prepared state when the draft/view changes | primary + M4 reviewer | Renderer regression covers differing live/saved goals, disabled export with cleared confirmation, and blocked shortcuts; focused set workspace passed 3/3 and post-review aggregate desktop passed 116/116 at `10d2511` |
| KI-067 | resolved in M4 review | medium, history and conflict semantics | Restore a saved version identical to the current head, or submit a stale entry mutation after another edit removed that entry | Identical restore returned a no-op instead of recording an undoable restore, while stale mutations validated against the newer head before checking the expected revision | Permit an explicit force-append for version restore and reject stale revisions before validating mutation-specific IDs against the current head | primary + M4 reviewer | Two focused core regressions failed before correction; repository/service passed 13/13 and post-review aggregate core passed 119/119 at `10d2511` |
| KI-068 | resolved in M4 final aggregate | medium, repeated-playlist analysis | Open a playlist ordered `(A, B, A)` and let the Library analysis status poll run | The renderer preserved repeated browsing rows but passed duplicate stable IDs to the strict analysis status boundary, which rejected the entire payload | De-duplicate polling and queue IDs after stable ordering, preserving the first occurrence and selected-first behavior | primary + renderer implementer | Focused regression first observed `[A, B, A]`, then analysis renderer passed 9/9 with status and queue assertions; the post-review M4 Electron flow and all five aggregate flows passed at `10d2511` |
| KI-069 | resolved in M4 final aggregate | low, integration synchronization | Perform several set mutations faster than the M4 E2E helper can observe them | The helper accepted any persistent `Draft updated.` text, so it could race into the next mutation and create a false optimistic-revision conflict | Announce the mutation type and resulting revision, and require each E2E wait to observe a new update message | primary | The prior aggregate failed at role editing after a stale success; focused M4 Electron flow and the complete five-flow aggregate passed after correction at `10d2511` |
| KI-070 | resolved in M5 integration | medium, keyboard accessibility | Close track details, delete a saved filter, or confirm/cancel preference export/reset using the keyboard | The first M5 controls removed their transient region but left focus on `body`, losing the user's place | Retain the initiating control/ref and restore focus to Details, Filter name, Prepare export, or Reset after the action closes | macOS UI specialist + primary | New focus assertions failed in the first complete desktop aggregate, then focused renderer passed 41/41 and complete desktop passed 136/136 at `b5cbee6` |
| KI-071 | resolved in M5 review | medium, visible preference state | Reset preferences while an already-loaded Next comparison is open | Reset changed durable preference state but the discovery panel retained and displayed its pre-reset personalized comparison until another control forced a fetch | Publish a successful-reset nonce from the Library screen, hide comparison results from an older nonce immediately, and refetch the open Next request in place | M5 reviewer + macOS UI specialist + primary | Focused regression first failed on the stale active status; post-fix personalization passed 6/6, renderer 42/42, and `pnpm verify:m5` passed 151 core, 137 desktop, strict typecheck/build, and six Electron flows |
